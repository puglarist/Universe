# Universe

Repository bootstrap for patch orchestration and startup tracking.

## Start-all pending patch batch

- Kickoff plan: `docs/patches/start_all_pending_patches.md`
- Registry: `config/patches/pending_patch_registry.json`
- Bootstrap script: `scripts/start_pending_patches.sh`

Run:

```bash
./scripts/start_pending_patches.sh
```
